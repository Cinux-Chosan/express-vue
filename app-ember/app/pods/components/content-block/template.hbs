  <div class="clearfix flex-all-center" local-class="header">
    <h1 class="text-center" local-class="title">{{title}}</h1>
    <div local-class="remark">{{remark}}</div>
  </div>
  <div class="m-20px pb-20px">
    {{#if hasBlock}}
      {{yield}}
    {{else}}
      {{content}}
    {{/if}}
  </div>